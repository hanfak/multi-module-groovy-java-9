description = "Retrieve data from whatever source (db, 3rd party, etc.)"

dependencies {
    compile project(":application:core")
    compile libs.database
    compile libs.clients

    testCompile libs.unit_tests
}